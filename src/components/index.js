import { getDocComponents } from '../../doc';

const doc = getDocComponents();

// TODO: loop over 'doc' and auto-generate the code below

import Button from './atoms/Button/Button';

export default {
    'src/components/atoms/Button/Button.jsx': Button
};
