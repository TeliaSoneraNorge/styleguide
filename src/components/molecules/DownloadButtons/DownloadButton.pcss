.download-buttons {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;

    &__button {
        display: block; /*Needed by certain rendering engines*/
        text-decoration: none;
    }

    &__image {
        display: block;
        max-width: 100%;
        width: 235px;
    }
}