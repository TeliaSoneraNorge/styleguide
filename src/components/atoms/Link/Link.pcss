@import "../../colors.pcss";

.link {
    border-bottom: 1px dotted var(--core-purple);
    color: var(--black);
    text-decoration: none;

    &:hover {
        border-bottom: 1px solid var(--core-purple);
    }
}
