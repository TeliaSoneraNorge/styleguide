.related-article {
    width: 267px;

    &__image {
        display: block;
        width: 100%;
    }

    &__text {
        background-color: var(--white);
        padding: 0.5rem 1rem 1rem;
    }

    &__heading {
        margin: 0;
    }
}