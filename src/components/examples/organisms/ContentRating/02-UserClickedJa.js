import React from 'react';
import { ContentRating } from 'component-lib';

const JAContentFeedback = () => (
    <ContentRating feedbackState="receipt-positive" />
);

export default JAContentFeedback;
