import React from 'react';
import { CheckBoxWithLabel } from '@telia/styleguide';

const Disabled = () => (
    <CheckBoxWithLabel label="Checkbox is disabled" disabled />
);

export default Disabled;