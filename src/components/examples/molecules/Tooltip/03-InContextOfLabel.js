import React from 'react';
import { Tooltip } from '@telia/styleguide';

const InContextOfLabel = () => (
  <Tooltip label="Clickable label" text="This is a tooltip in context of a label" isExpanded />
);

export default InContextOfLabel;
