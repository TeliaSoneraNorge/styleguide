import React from 'react';
import { TextBoxWithIcon } from 'component-lib';

const TypeSearch = () => (
    <TextBoxWithIcon
        type="search"
        iconName="ico_search"
        iconLabel="Search"
        iconIsButton />
);

export default TypeSearch;