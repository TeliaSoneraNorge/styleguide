import React from 'react';
import { StatefulBox } from '@telia/styleguide';

const SmallExpanded = () => (
  <StatefulBox color="teal" size="small" canExpand isExpanded>
    Example 5 - Small Expanded Box
  </StatefulBox>
);

export default SmallExpanded;
