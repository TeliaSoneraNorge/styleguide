import React from 'react';
import { ShadowBox } from 'component-lib';

const DefaultShadowBox = () => (
    <ShadowBox>This is a box with some content inside</ShadowBox>
);

export default DefaultShadowBox;