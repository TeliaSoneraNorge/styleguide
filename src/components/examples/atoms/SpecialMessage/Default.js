import React from 'react';
import { SpecialMessage } from '@telia/styleguide';

const DefaultSpecialMessage = () => (
  <SpecialMessage>
    This is a small block of text that should be used to highlight some important content on the page.
  </SpecialMessage>
);

export default DefaultSpecialMessage;
