import React from 'react';
import { ToggleButton } from '@telia/styleguide';

const WithLongLabelText = () => (
    <ToggleButton id='example-2' labelText='Example with a much longer label text, like really really long, yeah?'/>
);

export default WithLongLabelText;