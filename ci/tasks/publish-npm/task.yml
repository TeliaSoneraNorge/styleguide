---
platform: linux

image_resource:
  type: docker-image
  source: {repository: node, tag: "8-alpine" }

inputs:
  - name: styleguide

params:
  npm_token:

outputs:
  - name: styleguide

run:
  path: sh
  args:
  - styleguide/ci/tasks/publish-npm/task.sh
